namespace BookPromoterAI;

static class PromoConstants
{
    /// <summary>Max promo codes shown per table on the Owner page (most recent first).</summary>
    public const int MaxVisiblePromoCodes = 20;
}
